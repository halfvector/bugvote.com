<div class="l-horizontal l-stretch l-pull-right m-buglist-header">
    <div>
        <h1>Bugs and Suggestions</h1>
        <!--<form method="get" action="{{searchUrl}}" class="m-search-control">-->
            <!--<input class="field" type="text" name="q" placeholder="Search.." autocomplete="off" />-->
            <!--<button type="submit"><i class="icon-search"></i></button>-->
        <!--</form>-->
    </div>
    <div>
        <!--<a href="{{urlCreateSuggestion}}" class="button button-rounded button-flat-action"><i class="icon-plus-sign"></i> Submit Idea</a>-->
        <!--<a href="{{urlCreateSuggestion}}" class="btn btn-success"><i class="icon-plus-sign"></i> Submit Idea</a>-->
        <a href="{{urlCreateSuggestion}}" class="button button-rounded button-flat button-flat-action"><i class="icon-plus-sign"></i> Submit Idea</a>
    </div>
</div>

<div class="bootstrap bugs-list-nav">
        <ul class="nav nav-tabs">
            <li class="{{hottestClass}}"><a href="{{hottestIdeas}}">Hottest</a></li>
            <li class="{{newestClass}}"><a href="{{newestIdeas}}">Newest</a></li>
        </ul>
</div>

<section class="m-buglist">

    {{#suggestions}}
        {{> App/Components/BuglistItemStyle1 }}
    {{/suggestions}}

</section>

<script>
    function toggle_upvote(idea, vote) {
        var postData = "idea=" + idea + "&vote=" + vote;
        $.ajax({url: "{{urlVoteShortcut}}", type: "POST", data: postData})
         .done(function(data){

        });
    }
</script>